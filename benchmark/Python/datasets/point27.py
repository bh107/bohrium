import numpy as np

def gen_point27_data(size, name='heat'):
	"""TODO: Describe the data generated by this function."""
	
    data = np.zeros((size,size,size))
    mid = size/2
    data[mid-8:mid+8,mid-8:mid+8]=100.0
    
    return {name: data}
