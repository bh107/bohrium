#slurp
#compiler-settings
directiveStartToken = %
#end compiler-settings
%slurp
template <typename T>
inline
void assign_array_type( bh_array* array ) {};
// NOTE: The general implementation could output an error at runtime instead of failing at compile-time.

%for $ctype, $bh_atype, $bh_ctype, $bh_enum in $data
template <>
inline
void assign_array_type<$bh_atype>( bh_array* array )
{
    array->type = $bh_enum;
}

%end for
