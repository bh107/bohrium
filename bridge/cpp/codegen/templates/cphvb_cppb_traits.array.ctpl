#slurp
#compiler-settings
directiveStartToken = %
#end compiler-settings
%slurp
template <typename T>
inline
void assign_array_type( cphvb_array* array );
// NOTE: The general implementation could output an error at runtime instead of failing at compile-time.

%for $type in $data
template <>
inline
void assign_array_type<$type[1]>( cphvb_array* array )
{
    array->type = $type[3];
}

%end for
